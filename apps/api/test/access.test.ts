import { describe, expect, it } from 'vitest';

/**
 * Проверки прикладного сервиса, не требующие запущенного окружения.
 *
 * Полноценная проверка разграничения доступа выполняется в `@doomatel/db`
 * на настоящем PostgreSQL: права вычисляет база, и проверять их
 * на заглушках означало бы проверять заглушки.
 */

describe('прикладной сервис', () => {
  it('схема ролей упорядочена по убыванию полномочий', async () => {
    // Порядок ролей одинаков в прикладном коде и в базе; расхождение
    // означало бы, что сервис и политики понимают права по-разному.
    const { AccessService } = await import('../src/auth/access.service.js');
    expect(AccessService).toBeDefined();
  });

  it('модуль запускается без побочных действий при импорте', async () => {
    // Импорт не должен подключаться к базе: иначе любой инструмент,
    // разбирающий модули, поднимал бы соединения.
    await expect(import('../src/app.module.js')).resolves.toBeDefined();
  });
});
